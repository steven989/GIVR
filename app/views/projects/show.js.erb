$('.projects_detail').html("<%= j(render @project)  %><%= j(if current_user; if current_user.role == 'professional'; link_to 'Apply', project_applications_path(@project, todo: 'apply'), class: 'do_project', data: {todo: 'apply'}, method: 'POST' end end) %><%= j(if current_user; if current_user.role == 'professional'; link_to 'Shortlist', project_applications_path(@project, todo: 'shortlist'), class: 'do_project', data: {todo: 'shortlist'}, method: 'POST' end end) %><%= j( link_to 'Close', projects_path, id: 'close_project' ) %>");