<% if params[:page].nil? %>
  $('.projects_show').html("<%= j render partial: 'projects/project', collection: @projects, locals: {loc: 'index'} %>");
<% else %>
  $('.projects_show').append("<%= j render partial: 'projects/project', collection: @projects, locals: {loc: 'index'} %>");
<% end %>

$('.pagination_wrapper').html('<%= j paginate(@projects) %>');