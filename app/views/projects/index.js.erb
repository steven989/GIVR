<% if params[:page].nil? %>
  $('.projects_show').html("<%= j render(@projects) %>");
<% else %>
  $('.projects_show').append("<%= j render(@projects) %>");
<% end %>

$('.pagination_wrapper').html('<%= j paginate(@projects) %>');